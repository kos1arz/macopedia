{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.name, 'Nazwa', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.name) }}
    </div>
    <button type="submit" class="btn btn-primary">{{ button_label|default('Zapisz') }}</button>
{{ form_end(form) }}
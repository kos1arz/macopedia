<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="d-flex justify-content-center py-3 bg-white text-dark">
            <ul class="nav nav-pills">
                <li class="nav-item">
                    <a href="{{ path('product_index') }}" class="nav-link">Produkty</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('category_index') }}" class="nav-link">Kategorie</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('csv_import') }}" class="nav-link">Wgraj plik</a>
                </li>
            </ul>
        </header>

        {% block body %}{% endblock %}
    </body>
</html>
